apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-service
  namespace: crypto
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend-service
  template:
    metadata:
      labels:
        app: backend-service
    spec:
      containers:
        - name: backend
          image: arielsilberman/crypto-backend:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5001
          env:
            - name: MYSQL_HOST
              value: "mysqldb"
            - name: MYSQL_PORT
              value: "3306"
            - name: MYSQL_USER
              value: "root"
            - name: MYSQL_PASSWORD
              value: "123456"
            - name: MYSQL_DATABASE
              value: "crypto_db"
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: crypto
spec:
  type: ClusterIP
  selector:
    app: backend-service
  ports:
    - name: http
      port: 5001
      targetPort: 5001