apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mysql
  labels:
    app: {{ .Release.Name }}-mysql
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-mysql
  ports:
    - name: mysql
      port: {{ .Values.service.port }}
      targetPort: 3306